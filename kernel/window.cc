#include "vmm.h"
#include "window.h"

namespace WindowManagement {

    using namespace Generic;

    void init() {
        //LinkedList<Window, SpinLock> windows(Window(80, 1000));
        //windows.add_right(Window(80, 1000));

    }
}
